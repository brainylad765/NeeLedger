rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to projects collection (for testing)
    match /projects/{document} {
      allow read, write: if true;
    }
  }
}